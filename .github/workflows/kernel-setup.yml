name: Ne.app Kernel [SETUP]

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

env:
  BUILD_TYPE: KERNEL_BUILD

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Attempt NeBuild
      run: sudo curl https://git.src.nekernel.org/ne-build/releases/download/v0.0.6-apple-m1/ne-build-debian -o /usr/bin/nebuild && sudo chmod +x /usr/bin/nebuild
    - name: Check NeBuild
      run: nebuild -v
    - name: Install QEMU-IMG
      run: sudo apt update && sudo apt install qemu-utils
    - name: Attempt Kernel Setup
      run: mkdir tmp && git clone https://github.com/ne-foss-org/krnl.git tmp/kernel && cd tmp/kernel && sudo ./scripts/setup_x64_project.sh
